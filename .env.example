# Environment Configuration
NODE_ENV=development
REACT_APP_ENV=development

# Mock Data Control (Development Only)
# Set to 'true' to enable mock data fallbacks in development
# In production, mock data is NEVER used regardless of this setting
REACT_APP_USE_MOCK_DATA=false

# API Configuration
REACT_APP_API_URL=http://localhost:5000